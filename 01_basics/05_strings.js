const name = "sambit";
const repoCount = 11

console.log(name + repoCount); 

console.log(`My name is ${name} and I have ${repoCount} reositories`);

const game = new String('sambit');

console.log(game[2]);
console.log(game.length);
console.log(game.toUpperCase());
console.log(game.charAt(2));
console.log(game.indexOf('a'));

const newString = game.substring(0, 3);
console.log(newString);